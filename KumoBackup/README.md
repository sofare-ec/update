# KumoBackup 更新目录

此目录只服务 KumoBackup，不与仓库中的其他软件共享清单、安装包、校验文件或版本号。

- `update.json`：KumoBackup 客户端读取的当前版本及安装包地址。
- `releases/<版本>/`：每个已发布版本的完整安装包和 SHA-256 校验文件。
- 客户端只接受本目录下的 HTTPS 地址，并在安装前执行 SHA-256 校验。

其他软件必须使用各自的顶级目录，禁止修改或引用本目录中的文件。
