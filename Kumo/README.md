# Kumo 更新目录

此目录只服务 Kumo，不与仓库中的其他软件共享清单、安装包或版本号。

- `update.json`：客户端读取的当前版本和下载地址。
- `releases/<版本>/`：该版本的完整包、上一版本到当前版本的增量包，以及对应 SHA-256 文件。

其他软件应使用各自的顶级目录，禁止修改或引用本目录中的文件。

`.github/workflows/sync-kumo.yml` 每 15 分钟检查一次 Kumo 最新 GitHub Release，也支持手动运行。工作流只暂存并提交 `Kumo/` 路径，发现越界路径会立即失败。
